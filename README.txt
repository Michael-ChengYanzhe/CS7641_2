link : https://github.com/Michael-ChengYanzhe/CS7641_2.git

The package needs are:
python 3.6 or above
numpy 1.15.4
graphviz 2.40.1
pydotplus 2.0.2
scikit-learn 0.20.1
matplotlib 3.0.2
ABAGAIL
jython 2.7 or above

All the code files are in folder pythonCode. The only thing to run all code is to goto the pythonCode and run the jython files.

You may also run the script of run.sh, which will automatically run the jython script. 

The result data will be generated at outputData(for section 1), KS_out, CS_out and TSP_out(section 2). The script inside these folder can generate the visualization result of experiment.